
// Unlocks brainwashing when set.
FlagInfo.build('tasks.brainwash', {
  validateIn: ['unlocked'],
});

// Unlocks any crafting when set.
FlagInfo.build('tasks.craft', {
  validateIn: ['unlocked'],
});

// Unlocks experimentation when set.
FlagInfo.build('tasks.experiment', {
  validateIn: ['unlocked'],
});

// Unlocks meditation when set.
FlagInfo.build('tasks.meditate', {
  validateIn: ['unlocked'],
});

// Unlocks piss training when set.
FlagInfo.build('tasks.train.golden', {
  validateIn: ['unlocked'],
});
