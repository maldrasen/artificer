// global.Adjustment = class Adjustment extends Form {
//
//   // The apply() function should be overridden by the individual adjuster.
//   async apply(character) { throw "apply() must be overridden." }
//
// }
