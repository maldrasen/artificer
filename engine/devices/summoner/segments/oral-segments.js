Summoner.OralSegments = (function() {

  // This will have other segments, but the positioning functions were so
  // complex that I thought it best to move them into their own classes.
  return {};

})();
